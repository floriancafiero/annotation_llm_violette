# Colette — La Vagabonde (1910)

Manual annotation according to the current project codebook.

Source file used locally: `colette_vagabonde.txt`.

The Excel workbook in this folder contains:

- `Annotations`: retained rows;
- `Excluded candidates`: salient candidates deliberately not counted;
- `Codebook labels`: allowed labels for the current grid;
- `Notes`: provenance metadata and input hash.

This file was produced manually, not through an OpenRouter model run.
